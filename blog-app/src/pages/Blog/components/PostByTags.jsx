import React from 'react'
import toast from 'react-hot-toast'

const PostByTags = () => {
  return (
    <div>
      PostByTags

      <button
      className=''
      onClick={()=>toast.success("toast clicked")}
      >toast</button>
    </div>
  )
}

export default PostByTags
